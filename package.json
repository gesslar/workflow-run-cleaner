{
  "name": "workflow-run-cleaner",
  "version": "1.8.0",
  "description": "Clean up old workflow runs",
  "main": "cleanup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build cleanup.js -o dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.33.4"
  }
}
